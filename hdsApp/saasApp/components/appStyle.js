import {
    StyleSheet
} from 'react-native';

const appStyle = StyleSheet.create({
    labelStyle:{ paddingLeft: 10, paddingTop: 30, fontSize: 20, fontWeight:"bold" },
    inputStyle:{ paddingLeft: 10, paddingTop: 30, fontSize: 20 },
    singleFormElement: { width: "100%", flexDirection: 'row' ,  justifyContent: 'space-around'},
    buttonContainer: {margin: 20}
});

export default appStyle;